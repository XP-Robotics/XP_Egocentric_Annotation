# Stage 09 — Implementation
`attention_proxy.py <pipeline_result.pkl.gz> <frames_dir> <out_dir>`
Renders the manipulation-attention proxy (fingertip/active-object centroid + trail).
True gaze requires an eye-tracking stream (hardware-gated) — this is a documented
proxy, not measured gaze.
