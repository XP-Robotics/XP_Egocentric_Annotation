# Stage 10 — Implementation
`render_neurodepth.py <frames_dir> <out_mp4> [--fps 30 --thresh 0.12 --tau 6]`
Simulates a neuromorphic event camera from RGB: ON/OFF events (log-intensity
polarity), time surface (decaying last-event-time), reconstructed edges, + event HUD.
